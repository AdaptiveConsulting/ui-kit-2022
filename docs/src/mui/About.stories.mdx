import { Meta } from '@storybook/addon-docs';

<Meta title="Material UI / About" />

# Material UI

This section demonstrates how components imported from `@mui/material` will manifest themselves when they are used in an application using the toolkit theme. We do not have toolkit specific versions for these components, but they should look aligned to the design system specs automatically through theme overrides.
